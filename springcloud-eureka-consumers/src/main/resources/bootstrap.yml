server:
  port: 8081
spring:
  application:
    name: spring-cloud-consumers
  cloud:
    config:
      name: springcloud-config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config
# 熔断器 调用服务失败后降级操作
feign:
  hystrix:
    enabled: true